{
  "name": "me_yourmeal_vite",
  "private": true,
  "version": "0.0.01",
  "type": "module",
  "scripts": {
    "test": "echo Halo test this is test of layout",
    "version": "echo node current version: && node -v && echo npm current version: && npm -v",
    "dev": "vite",
    "open": "vite --open",
    "start": "vite --open",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@a1rth/css-normalize": "^1.2.1",
    "normalize.css": "^8.0.1"
  }
}
